[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
torch = ">=2.0.0"
torchvision = ">=0.15.0"
numpy = ">=1.21.0"
pillow = ">=8.3.1"
flask = ">=2.0.1"
tqdm = ">=4.62.0"
matplotlib = ">=3.4.3"
scikit-learn = ">=0.24.2"
transformers = ">=4.30.0"

[dev-packages]

[requires]
python_version = "3.11"
